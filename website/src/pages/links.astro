---
// src/pages/links.astro

// 1. OFERTAS FLASH (CON FOTO Y PRECIO) - Se verán como tarjetas arriba
const flashOffers = [
    {
        name: "Altavoz Ducha Impermeable",
        desc: "Ventosa fuerte, sonido HD y 100% resistente al agua.",
        price: "10,44€",
        oldPrice: "19,99€",
        discount: "-48%",
        img: "https://m.media-amazon.com/images/I/71n+D0vaORL._AC_SX679_.jpg",
        url: "https://amzn.to/4qDXjnv" // <--- TU LINK DEL ALTAVOZ
    },
    {
        name: "Ratón Logitech B100",
        desc: "El clásico de oficina. Indestructible y preciso.",
        price: "4,95€",
        oldPrice: "12,99€",
        discount: "-60%",
        img: "https://m.media-amazon.com/images/I/61hzuoXwjqL._AC_SY300_SX300_QL70_ML2_.jpg",
        url: "https://amzn.to/49iQsJ7" 
    },
    {
        name: "Portátil Estudiantes 14''",
        desc: "Windows 11, 8GB RAM. Ideal ofimática y Netflix.",
        price: "279€",
        oldPrice: "999€",
        discount: "-70%",
        img: "https://m.media-amazon.com/images/I/71WQY0brEFL._AC_SX679_.jpg",
        url: "https://amzn.to/4qpBAPZ"
    }
];

// 2. ENLACES DE NAVEGACIÓN (SIN FOTO) - Se verán como botones abajo
const navLinks = [
  {
    title: "✈️ Únete al Canal de Telegram",
    subtitle: "Avisos de error de precio y chollos diarios",
    url: "https://t.me/PreciosPortatilesES",
    type: "telegram",
    icon: "📲"
  },
  {
    title: "🔍 Ir al Comparador Web",
    subtitle: "Analiza y compara portátiles sin engaños",
    url: "/", 
    type: "standard",
    icon: "💻"
  }
];
---

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Enlaces | MasterTech</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;700;800&display=swap" rel="stylesheet">

    <script async src="https://www.googletagmanager.com/gtag/js?id=G-DHN4XB4R6G"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-DHN4XB4R6G');
    </script>

    <style>
        :root {
            --bg-dark: #0f172a;
            --text-main: #ffffff;
            --text-muted: #94a3b8;
            --accent-red: #ef4444;
        }

        body {
            margin: 0; padding: 0;
            font-family: 'Plus Jakarta Sans', sans-serif;
            background-color: var(--bg-dark);
            background: linear-gradient(-45deg, #0f172a, #1e1b4b, #312e81, #0f172a);
            background-size: 400% 400%;
            animation: gradientBG 15s ease infinite;
            color: var(--text-main);
            min-height: 100vh;
            display: flex; flex-direction: column; align-items: center;
        }

        .container {
            width: 100%; max-width: 480px; padding: 40px 20px;
            display: flex; flex-direction: column; align-items: center; gap: 15px;
            box-sizing: border-box;
        }

        /* HEADER PERFIL */
        .profile-img-container {
            width: 90px; height: 90px; border-radius: 50%; padding: 3px;
            background: linear-gradient(to right, #3b82f6, #8b5cf6); margin-bottom: 10px;
        }
        .profile-img {
            width: 100%; height: 100%; background-color: #1e293b;
            border-radius: 50%; display: flex; align-items: center; justify-content: center;
            font-size: 35px; border: 3px solid #0f172a;
        }
        h1 { margin: 0; font-size: 1.4rem; font-weight: 800; letter-spacing: -0.5px; }
        p.bio { color: var(--text-muted); text-align: center; font-size: 0.95rem; line-height: 1.5; margin-top: 5px; margin-bottom: 10px; }

        /* 🔥 ESTILOS OFERTAS FLASH (TARJETAS VISUALES) 🔥 */
        .flash-section {
            width: 100%;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(239, 68, 68, 0.3);
            border-radius: 20px; padding: 15px;
            box-sizing: border-box; margin-bottom: 10px;
        }

        .flash-header {
            display: flex; align-items: center; justify-content: center; gap: 8px;
            color: #ef4444; font-weight: 800; font-size: 0.8rem;
            margin-bottom: 15px; letter-spacing: 1px; text-transform: uppercase;
        }
        .pulse-dot {
            width: 8px; height: 8px; background: #ef4444; border-radius: 50%;
            box-shadow: 0 0 8px #ef4444; animation: pulse 1.5s infinite;
        }

        .flash-card {
            display: flex; align-items: center; gap: 15px;
            background: rgba(30, 41, 59, 0.6);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 16px; padding: 12px;
            text-decoration: none; color: white;
            margin-bottom: 12px; position: relative;
            transition: transform 0.2s;
        }
        .flash-card:last-child { margin-bottom: 0; }
        .flash-card:hover { transform: scale(1.02); background: rgba(30, 41, 59, 0.9); border-color: #ef4444; }

        .flash-img {
            width: 70px; height: 70px; object-fit: contain;
            background: white; border-radius: 12px; padding: 5px; flex-shrink: 0;
        }
        .flash-info h3 { margin: 0 0 4px; font-size: 0.95rem; font-weight: 700; line-height: 1.2; }
        .flash-desc { font-size: 0.75rem; color: #94a3b8; margin: 0 0 8px; line-height: 1.3; }
        
        .price-row { display: flex; align-items: center; gap: 8px; font-size: 0.85rem; }
        .old-price { color: #64748b; text-decoration: line-through; font-size: 0.8rem;}
        .new-price { color: #ef4444; font-weight: 800; font-size: 1.1rem; }
        
        .discount-badge {
            margin-left: auto; background: #ef4444; color: white;
            font-size: 0.75rem; font-weight: 800; padding: 4px 8px; border-radius: 6px;
        }

        /* ESTILOS BOTONES NORMALES (Telegram/Web) */
        .link-card {
            width: 100%; text-decoration: none; padding: 16px 20px;
            border-radius: 16px; display: flex; align-items: center; gap: 15px;
            transition: transform 0.2s, box-shadow 0.2s; position: relative;
            overflow: hidden; box-sizing: border-box; margin-bottom: 12px;
        }
        .link-card:hover { transform: translateY(-4px) scale(1.02); box-shadow: 0 10px 20px rgba(0,0,0,0.3); }
        
        .telegram { background: linear-gradient(135deg, #0088cc 0%, #005f8f 100%); color: white; border: 1px solid rgba(255,255,255,0.1); }
        .standard { background: rgba(30, 41, 59, 0.7); backdrop-filter: blur(10px); border: 1px solid rgba(255, 255, 255, 0.1); color: white; }
        
        .icon { font-size: 1.5rem; }
        .text-content { display: flex; flex-direction: column; }
        .link-title { font-weight: 700; font-size: 1rem; line-height: 1.2; }
        .link-subtitle { font-size: 0.75rem; opacity: 0.8; margin-top: 2px; }

        footer { margin-top: auto; padding: 30px 20px; text-align: center; font-size: 0.75rem; color: #475569; }
        
        @keyframes gradientBG { 0% { background-position: 0% 50%; } 50% { background-position: 100% 50%; } 100% { background-position: 0% 50%; } }
        @keyframes pulse { 0% { opacity: 1; } 50% { opacity: 0.4; } 100% { opacity: 1; } }
    </style>
</head>
<body>

    <div class="container">
        <div class="profile-img-container"><div class="profile-img">⚡</div></div>
        <div>
            <h1 style="text-align: center;">@MasterTech</h1>
            <p class="bio">Ofertas verificadas de tecnología.<br>👇 <b>CHOLLOS ACTIVOS</b> 👇</p>
        </div>

        <div class="flash-section">
            <div class="flash-header">
                <div class="pulse-dot"></div><span>OFERTAS FLASH (24H)</span><div class="pulse-dot"></div>
            </div>
            
            {flashOffers.map(offer => (
                <a href={offer.url} target="_blank" rel="noopener noreferrer" class="flash-card">
                    <img src={offer.img} alt={offer.name} class="flash-img">
                    <div class="text-content" style="flex: 1;">
                        <h3>{offer.name}</h3>
                        <p class="flash-desc">{offer.desc}</p>
                        <div class="price-row">
                            <span class="old-price">{offer.oldPrice}</span>
                            <span class="new-price">{offer.price}</span>
                        </div>
                    </div>
                    <div class="discount-badge">{offer.discount}</div>
                </a>
            ))}
        </div>

        {navLinks.map(link => (
            <a href={link.url} target="_blank" rel="noopener noreferrer" class={`link-card ${link.type}`}>
                <span class="icon">{link.icon}</span>
                <div class="text-content">
                    <span class="link-title">{link.title}</span>
                    {link.subtitle && <span class="link-subtitle">{link.subtitle}</span>}
                </div>
            </a>
        ))}
    </div>

    <footer>
        <p>Participamos en el Programa de Afiliados de Amazon.es.</p>
        <p style="margin-top: 10px;">&copy; {new Date().getFullYear()} MasterTech</p>
    </footer>

</body>
</html>