---
// TUS ENLACES (Configúralos aquí)
const links = [
  {
    title: "🔥 ¡OFERTA TIKTOK: PORTÁTIL 279€!",
    subtitle: "Antes 900€ (Marketing) - Ideal Estudiantes",
    url: "https://amzn.to/4qpBAPZ",
    type: "featured", // Estilo rojo destacado
    icon: "💣"
  },
  {
    title: "✈️ Únete al Canal de Telegram",
    subtitle: "Avisos de error de precio y chollos diarios",
    url: "https://t.me/PreciosPortatilesES",
    type: "telegram", // Estilo azul
    icon: "📲"
  },
  {
    title: "💻 Ir al Comparador Web",
    subtitle: "Analiza y compara portátiles sin engaños",
    url: "/",
    type: "standard", // Estilo oscuro
    icon: "🔍"
  }
];
---

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Enlaces | LaptopComparator</title>
    <meta name="description" content="Enlaces destacados de LaptopComparator">
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;700;800&display=swap" rel="stylesheet">

    <script async src="https://www.googletagmanager.com/gtag/js?id=G-DHN4XB4R6G"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-DHN4XB4R6G');
    </script>

    <style>
        :root {
            --bg-dark: #0f172a;
            --text-main: #ffffff;
            --text-muted: #94a3b8;
        }

        body {
            margin: 0;
            padding: 0;
            font-family: 'Plus Jakarta Sans', sans-serif;
            background-color: var(--bg-dark);
            background: linear-gradient(-45deg, #0f172a, #1e1b4b, #312e81, #0f172a);
            background-size: 400% 400%;
            animation: gradientBG 15s ease infinite;
            color: var(--text-main);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        /* Contenedor principal */
        .container {
            width: 100%;
            max-width: 480px; /* Ancho típico de móvil */
            padding: 40px 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 20px;
        }

        /* Perfil */
        .profile-img-container {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            padding: 3px;
            background: linear-gradient(to right, #3b82f6, #8b5cf6);
            margin-bottom: 10px;
        }
        
        .profile-img {
            width: 100%;
            height: 100%;
            background-color: #1e293b;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 40px;
            border: 3px solid #0f172a; /* Borde falso para separar */
        }

        h1 { margin: 0; font-size: 1.25rem; font-weight: 800; letter-spacing: -0.5px; }
        p.bio { color: var(--text-muted); text-align: center; font-size: 0.9rem; line-height: 1.5; margin-top: 5px; margin-bottom: 20px; }

        /* Botones de Enlace */
        .link-card {
            width: 100%;
            text-decoration: none;
            padding: 16px 20px;
            border-radius: 16px;
            display: flex;
            align-items: center;
            gap: 15px;
            transition: transform 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94), box-shadow 0.2s;
            position: relative;
            overflow: hidden;
            box-sizing: border-box; /* Para que el padding no rompa el ancho */
        }

        .link-card:hover {
            transform: translateY(-4px) scale(1.02);
            box-shadow: 0 10px 20px rgba(0,0,0,0.3);
        }

        /* Estilos específicos de botones */
        .featured {
            background: linear-gradient(135deg, #dc2626 0%, #991b1b 100%);
            color: white;
            box-shadow: 0 4px 15px rgba(220, 38, 38, 0.4);
            border: 1px solid rgba(255,255,255,0.2);
            animation: pulse-border 2s infinite;
        }

        .telegram {
            background: linear-gradient(135deg, #0088cc 0%, #005f8f 100%); /* Azul Telegram */
            color: white;
            border: 1px solid rgba(255,255,255,0.1);
        }

        .standard {
            background: rgba(30, 41, 59, 0.7); /* Glassmorphism oscuro */
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: white;
        }
        .standard:hover {
            background: rgba(255, 255, 255, 0.1);
        }

        /* Texto dentro de los botones */
        .icon { font-size: 1.5rem; }
        .text-content { display: flex; flex-direction: column; }
        .link-title { font-weight: 700; font-size: 1rem; line-height: 1.2; }
        .link-subtitle { font-size: 0.75rem; opacity: 0.8; margin-top: 2px; font-weight: 400; }

        /* Footer */
        footer {
            margin-top: auto;
            padding: 30px 20px;
            text-align: center;
            font-size: 0.7rem;
            color: #475569;
            max-width: 400px;
        }

        /* Animaciones */
        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }
        @keyframes pulse-border {
            0% { box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.7); }
            70% { box-shadow: 0 0 0 10px rgba(220, 38, 38, 0); }
            100% { box-shadow: 0 0 0 0 rgba(220, 38, 38, 0); }
        }

    </style>
</head>
<body>

    <div class="container">
        <div class="profile-img-container">
            <div class="profile-img">⚡</div>
        </div>

        <div>
            <h1 style="text-align: center;">@LaptopComparator</h1>
            <p class="bio">
                Encuentra las mejores ofertas de portátiles sin marketing engañoso.
                <br>👇 <b>¡OFERTAS DEL DÍA!</b> 👇
            </p>
        </div>

        {links.map(link => (
            <a href={link.url} target="_blank" rel="noopener noreferrer" class={`link-card ${link.type}`}>
                <span class="icon">{link.icon}</span>
                <div class="text-content">
                    <span class="link-title">{link.title}</span>
                    {link.subtitle && <span class="link-subtitle">{link.subtitle}</span>}
                </div>
            </a>
        ))}
    </div>

    <footer>
        <p>Participamos en el Programa de Afiliados de Amazon.es. Obtenemos ingresos por las compras adscritas que cumplen los requisitos.</p>
        <p style="margin-top: 10px;">&copy; {new Date().getFullYear()} LaptopComparator</p>
    </footer>

</body>
</html>