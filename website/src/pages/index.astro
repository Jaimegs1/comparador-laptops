---
import laptops from '../data/laptops.json';

function getScore(price) {
    if (price > 1200) return 99;
    if (price > 900) return 97;
    if (price > 700) return 94;
    return 89;
}
---

<html lang="es-ES">
<head>
    <meta charset="utf-8" />
    
    <meta name="geo.region" content="ES" />
    <meta name="geo.position" content="40.416775;-3.703790" />
    <meta name="ICBM" content="40.416775, -3.703790" />

    <script async src="https://www.googletagmanager.com/gtag/js?id=G-DHN4XB4R6G"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-DHN4XB4R6G');
    </script>

    <script type="text/javascript">
        (function(c,l,a,r,i,t,y){
            c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
            t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
            y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
        })(window, document, "clarity", "script", "utlp0yi5s1");
    </script>

    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Laptop Comparator - Encuentra tu Portátil Ideal</title>
    <meta name="description" content="Comparador de portátiles en España. Ofertas Gaming, Estudiantes y Ultrabooks. Análisis técnico sin marketing." />
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;700;800&display=swap" rel="stylesheet">

    <style>
        :root {
            --bg-body: #0f172a;
            --bg-footer: #020617; 
            --card-bg: rgba(255, 255, 255, 0.05);
            --card-border: rgba(255, 255, 255, 0.1);
            --text-main: #ffffff;
            --text-muted: #cbd5e1;
            --text-legal: #64748b;
            --accent: #818cf8; 
            --gold: #fbbf24; /* Color Dorado para el ganador */
        }
        body { font-family: 'Plus Jakarta Sans', sans-serif; background-color: var(--bg-body); color: var(--text-main); margin: 0; padding: 0; min-height: 100vh; overflow-x: hidden; display: flex; flex-direction: column; width: 100%; }
        
        /* NAVBAR */
        .navbar { position: fixed; top: 20px; left: 50%; transform: translateX(-50%); width: 90%; max-width: 1100px; display: flex; justify-content: space-between; align-items: center; padding: 12px 30px; background: rgba(15, 23, 42, 0.85); backdrop-filter: blur(12px); border: 1px solid rgba(255, 255, 255, 0.1); border-radius: 100px; z-index: 1000; box-shadow: 0 10px 30px rgba(0,0,0,0.3); box-sizing: border-box; }
        .logo { font-size: 1.1rem; font-weight: 800; color: white; cursor: pointer; text-decoration: none; white-space: nowrap; }
        
        .nav-links { display: flex; gap: 25px; font-size: 0.85rem; font-weight: 600; color: #e2e8f0; align-items: center; }
        .nav-btn { cursor: pointer; transition: color 0.3s; }
        .nav-btn:hover { color: white; }
        .nav-vs-link { color: #4ade80; text-decoration: none; display: flex; align-items: center; gap: 5px; transition: transform 0.2s; }
        .nav-vs-link:hover { transform: scale(1.05); color: #86efac; }
        
        /* HERO (Titular Agresivo) */
        .hero { position: relative; min-height: 85vh; display: flex; flex-direction: column; justify-content: center; align-items: center; text-align: center; padding: 0 20px; overflow: hidden; background: radial-gradient(at 0% 0%, #4c1d95 0px, transparent 50%), radial-gradient(at 100% 0%, #3b82f6 0px, transparent 50%), #1e1b4b; border-bottom: 1px solid rgba(255,255,255,0.1); }
        .badge-pill { background: rgba(255, 255, 255, 0.1); border: 1px solid rgba(255, 255, 255, 0.2); color: #e0e7ff; padding: 6px 16px; border-radius: 50px; font-size: 0.8rem; font-weight: 600; margin-bottom: 30px; backdrop-filter: blur(4px); }
        
        h1 { font-size: 4rem; font-weight: 800; margin: 0; line-height: 1.1; letter-spacing: -2px; color: #ffffff; text-shadow: 0 10px 30px rgba(0,0,0,0.3); }
        h1 span { color: #818cf8; }

        p.subtitle { color: #e2e8f0; font-size: 1.2rem; max-width: 600px; margin: 30px auto 45px; line-height: 1.6; }
        
        .cta-container { display: flex; gap: 20px; flex-wrap: wrap; justify-content: center; align-items: center; }
        .btn-telegram-hero { 
            background: linear-gradient(135deg, #2AABEE 0%, #229ED9 100%);
            color: white; padding: 16px 35px; border-radius: 100px; font-weight: 800; font-size: 1rem;
            text-decoration: none; border: none; cursor: pointer; transition: transform 0.2s, box-shadow 0.2s; 
            box-shadow: 0 10px 30px rgba(42, 171, 238, 0.3); display: flex; align-items: center; gap: 10px;
        }
        .btn-telegram-hero:hover { transform: scale(1.05) translateY(-3px); box-shadow: 0 15px 40px rgba(42, 171, 238, 0.5); }
        .btn-compare-hero {
            background: white; color: #0f172a; padding: 16px 35px; border-radius: 100px; font-weight: 800; font-size: 1rem;
            text-decoration: none; border: none; cursor: pointer; transition: transform 0.2s, box-shadow 0.2s; 
            box-shadow: 0 10px 30px rgba(255, 255, 255, 0.1);
        }
        .btn-compare-hero:hover { transform: scale(1.05) translateY(-3px); background-color: #f8fafc; }

        /* 🔥 OFERTA FLASH (Ratón) 🔥 */
        .flash-deal-wrapper { width: 100%; display: flex; justify-content: center; margin-top: -40px; padding: 0 20px; position: relative; z-index: 10; box-sizing: border-box; }
        .flash-deal-card {
            background: linear-gradient(90deg, #1e293b 0%, #0f172a 100%); border: 1px solid #dc2626; box-shadow: 0 0 20px rgba(220, 38, 38, 0.3);
            width: 100%; max-width: 900px; border-radius: 20px; padding: 15px 30px; display: flex; align-items: center; justify-content: space-between;
            text-decoration: none; transition: transform 0.2s, box-shadow 0.2s; position: relative; overflow: hidden; box-sizing: border-box;
        }
        .flash-deal-card:hover { transform: translateY(-5px); box-shadow: 0 0 30px rgba(220, 38, 38, 0.6); background: linear-gradient(90deg, #283548 0%, #172033 100%); }
        .flash-badge { position: absolute; top: 0; left: 0; background: #dc2626; color: white; font-size: 0.7rem; font-weight: 800; padding: 4px 12px; border-bottom-right-radius: 10px; text-transform: uppercase; animation: pulse-red 2s infinite; }
        .flash-content { display: flex; align-items: center; gap: 20px; }
        .flash-icon { font-size: 2.5rem; }
        .flash-text h3 { margin: 0; color: #fca5a5; font-size: 1.1rem; font-weight: 800; }
        .flash-text p { margin: 4px 0 0 0; color: #94a3b8; font-size: 0.85rem; max-width: 400px; }
        .flash-price { display: flex; flex-direction: column; align-items: flex-end; justify-content: center; }
        .old-price { text-decoration: line-through; color: #64748b; font-size: 0.8rem; }
        .new-price { color: #ef4444; font-size: 1.5rem; font-weight: 800; }
        .btn-grab { background: white; color: #dc2626; font-size: 0.7rem; font-weight: 800; padding: 4px 10px; border-radius: 4px; margin-top: 5px; }
        @keyframes pulse-red { 0% { opacity: 1; } 50% { opacity: 0.7; } 100% { opacity: 1; } }

        /* MAIN & SIDEBAR */
        .main-container { width: 100%; max-width: 1400px; margin: 0 auto; padding: 60px 20px 100px; display: flex; gap: 50px; flex: 1; box-sizing: border-box; }
        .sidebar { width: 260px; display: none; flex-shrink: 0; }
        @media (min-width: 1000px) { .sidebar { display: block; } }
        .filter-box { margin-bottom: 40px; }
        .filter-title { font-weight: 700; margin-bottom: 20px; display: block; font-size: 0.8rem; text-transform: uppercase; color: #94a3b8; }
        .filter-item { display: block; color: var(--text-muted); margin-bottom: 14px; cursor: pointer; transition: 0.2s; }
        .filter-item:hover { color: white; transform: translateX(5px); }

        .grid { flex: 1; display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 30px; width: 100%; }

        /* CARD (Tarjeta de Producto) */
        .card { 
            background: linear-gradient(145deg, rgba(30, 41, 59, 0.7) 0%, rgba(30, 41, 59, 0.4) 100%); 
            border: 1px solid var(--card-border); 
            border-radius: 24px; padding: 30px; 
            position: relative; display: flex; flex-direction: column; 
            transition: 0.3s; 
        }
        .card:hover { transform: translateY(-8px); border-color: #818cf8; box-shadow: 0 20px 40px -10px rgba(0,0,0,0.4); background: rgba(30, 41, 59, 0.9); }
        
        /* 🏆 ESTILOS DEL GANADOR (Highlight) 🏆 */
        .winner-card {
            border: 2px solid var(--gold);
            box-shadow: 0 0 30px rgba(251, 191, 36, 0.15);
            background: linear-gradient(145deg, rgba(30, 41, 59, 0.9) 0%, rgba(66, 50, 15, 0.2) 100%);
            transform: scale(1.02); /* Ligeramente más grande */
        }
        .winner-badge-label {
            position: absolute; top: -14px; left: 50%; transform: translateX(-50%);
            background: var(--gold); color: #0f172a;
            font-size: 0.7rem; font-weight: 800; padding: 6px 16px; border-radius: 50px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.4); z-index: 30; white-space: nowrap; text-transform: uppercase;
        }

        .score-badge { position: absolute; top: 20px; right: 20px; width: 50px; height: 50px; background: #3b82f6; color: white; border-radius: 14px; display: flex; flex-direction: column; align-items: center; justify-content: center; font-weight: 800; z-index: 20; box-shadow: 0 4px 10px rgba(0,0,0,0.3); }
        .score-label { font-size: 0.6rem; font-weight: 600; text-transform: uppercase; }
        
        .product-img { width: 100%; height: 220px; object-fit: contain; margin: 15px 0 30px; background: #ffffff; border-radius: 16px; padding: 20px; box-sizing: border-box; position: relative; z-index: 1; transition: transform 0.3s ease; }
        .product-img:hover { transform: scale(1.02); }

        h2 { font-size: 1.3rem; font-weight: 700; margin: 0 0 15px 0; height: 3em; overflow: hidden; line-height: 1.4; }
        .specs-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; margin-bottom: 30px; background: rgba(0,0,0,0.2); padding: 15px; border-radius: 16px; }
        .spec-item { font-size: 0.85rem; color: #cbd5e1; display: flex; gap: 8px; align-items: center;}
        
        .price-row { margin-top: auto; display: flex; justify-content: space-between; align-items: center; padding-top: 20px; border-top: 1px solid rgba(255,255,255,0.05); }
        .price-container { display: flex; flex-direction: column; align-items: flex-start; }
        .old-price-anchor { text-decoration: line-through; color: #64748b; font-size: 0.85rem; margin-bottom: 0px; }
        .price-display { font-size: 1.6rem; font-weight: 800; color: white; }
        .btn-card { background: white; color: #0f172a; padding: 12px 24px; border-radius: 10px; font-weight: 700; text-decoration: none; font-size: 0.9rem; transition: background 0.2s; }
        .btn-card:hover { background: #e2e8f0; }

        /* FOOTER */
        .site-footer { background-color: var(--bg-footer); border-top: 1px solid var(--card-border); padding: 60px 20px 40px; margin-top: auto; width: 100%; box-sizing: border-box; }
        .footer-content { max-width: 1100px; margin: 0 auto; display: flex; flex-direction: column; gap: 30px; align-items: center; text-align: center; }
        .footer-brand h3 { margin: 0; font-size: 1.2rem; color: white; }
        .footer-brand p { font-size: 0.9rem; color: var(--text-muted); margin-top: 5px; }
        .footer-links { display: flex; gap: 20px; font-size: 0.9rem; }
        .footer-links a { color: var(--text-muted); text-decoration: none; transition: 0.2s; }
        .footer-links a:hover { color: white; }
        .amazon-legal-box { max-width: 800px; margin: 40px auto 0; padding-top: 30px; border-top: 1px solid rgba(255,255,255,0.05); text-align: center; }
        .legal-text { font-size: 0.75rem; color: var(--text-legal); line-height: 1.6; margin: 0; }

        /* WIZARD & MÓVIL (Styles) */
        .wizard-fab { position: fixed; bottom: 30px; right: 30px; background: linear-gradient(135deg, #6366f1 0%, #a855f7 100%); color: white; padding: 15px 25px; border-radius: 50px; font-weight: 800; font-size: 1rem; box-shadow: 0 10px 25px rgba(124, 58, 237, 0.4); cursor: pointer; z-index: 100; transition: transform 0.3s, box-shadow 0.3s; display: flex; align-items: center; gap: 10px; }
        .wizard-fab:hover { transform: scale(1.05) translateY(-3px); box-shadow: 0 15px 35px rgba(124, 58, 237, 0.6); }
        .wizard-modal { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(15, 23, 42, 0.9); backdrop-filter: blur(10px); z-index: 200; display: none; align-items: center; justify-content: center; opacity: 0; transition: opacity 0.3s ease; }
        .wizard-modal.active { display: flex; opacity: 1; }
        .wizard-content { background: #1e293b; border: 1px solid #334155; border-radius: 24px; width: 90%; max-width: 500px; padding: 40px; position: relative; text-align: center; box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5); transform: translateY(20px); transition: transform 0.3s ease; }
        .wizard-modal.active .wizard-content { transform: translateY(0); }
        .step-container { display: none; }
        .step-container.active { display: block; animation: fadeIn 0.4s ease; }
        .wizard-title { font-size: 1.5rem; font-weight: 800; margin-bottom: 20px; color: white; }
        .wizard-btn { display: block; width: 100%; padding: 15px; margin-bottom: 10px; background: #334155; border: 1px solid #475569; border-radius: 12px; color: white; font-weight: 600; cursor: pointer; transition: 0.2s; text-align: left; }
        .wizard-btn:hover { background: #475569; border-color: #818cf8; transform: translateX(5px); }
        .result-card { display: none; margin-top: 20px; }
        .result-card.show { display: block; }
        .wizard-close { position: absolute; top: 20px; right: 20px; background: none; border: none; color: #94a3b8; font-size: 1.5rem; cursor: pointer; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

        @media (min-width: 768px) { h1 { font-size: 3rem; } .navbar { width: 85%; } .footer-content { flex-direction: row; justify-content: space-between; text-align: left; } }
        @media (max-width: 768px) {
            .navbar { width: 95%; padding: 10px 15px; } .logo { font-size: 1rem; } .nav-links { display: none; }
            .hero { min-height: auto; padding-top: 100px; padding-bottom: 40px; }
            h1 { font-size: 2.3rem !important; line-height: 1.1 !important; margin-bottom: 15px !important; word-break: keep-all; }
            p.subtitle { font-size: 1rem !important; line-height: 1.4; padding: 0 15px; margin-bottom: 25px !important; }
            .cta-container { flex-direction: column; width: 100%; gap: 15px; }
            .btn-telegram-hero, .btn-compare-hero { width: 90%; justify-content: center; font-size: 0.95rem; padding: 14px 20px; }
            .flash-deal-wrapper { margin-top: 0; padding: 0 15px; }
            .flash-deal-card { flex-direction: column; text-align: center; gap: 15px; padding: 20px; margin-top: 20px; }
            .flash-content { flex-direction: column; gap: 5px; }
            .flash-text p { display: none; } .flash-price { align-items: center; width: 100%; border-top: 1px solid rgba(255,255,255,0.1); padding-top: 10px; margin-top: 5px; }
            .wizard-fab { bottom: 20px !important; right: 20px !important; font-size: 0.9rem !important; padding: 12px 20px !important; z-index: 9999; }
        }
    </style>
</head>
<body>
    <button id="wizard-trigger" class="wizard-fab">🧙‍♂️ ¿Te ayudo a elegir?</button>

    <div id="wizard-overlay" class="wizard-modal">
        <div class="wizard-content">
            <button id="close-wizard" class="wizard-close">&times;</button>
            <div id="step-1" class="step-container active">
                <h3 class="wizard-title">💰 ¿Cuál es tu presupuesto máximo?</h3>
                <button class="wizard-btn" onclick="nextStep(1, 500)">📉 Menos de 500€</button>
                <button class="wizard-btn" onclick="nextStep(1, 800)">⚖️ Entre 500€ y 800€</button>
                <button class="wizard-btn" onclick="nextStep(1, 2000)">🚀 Más de 1000€ (Sin límite)</button>
            </div>
            <div id="step-2" class="step-container">
                <h3 class="wizard-title">🎯 ¿Para qué lo vas a usar?</h3>
                <button class="wizard-btn" onclick="nextStep(2, 'student')">🎓 Universidad / Ofimática / Netflix</button>
                <button class="wizard-btn" onclick="nextStep(2, 'gaming')">🎮 Gaming (Jugar a todo)</button>
                <button class="wizard-btn" onclick="nextStep(2, 'code')">💻 Programación / Edición Video</button>
            </div>
            <div id="step-result" class="step-container">
                <h3 class="wizard-title">✨ Tu Portátil Ideal es:</h3>
                <div id="wizard-loading">Calculando...</div>
                <div id="wizard-match" class="result-card">
                    <img id="wiz-img" src="" style="width: 150px; margin: 0 auto; display:block;" />
                    <h4 id="wiz-name" style="margin: 10px 0; color: #818cf8;"></h4>
                    <p id="wiz-price" style="font-size: 1.5rem; font-weight: 800; color: white;"></p>
                    <a id="wiz-link" href="#" class="btn-amazon" style="display:block; margin-top:15px; background: #3b82f6; padding: 12px; border-radius: 8px; color: white; text-decoration: none; font-weight: bold; transition: 0.3s;">🛒 Ver Oferta en Amazon</a>
                </div>
                <button onclick="resetWizard()" style="margin-top: 20px; background: none; border: none; color: #94a3b8; cursor: pointer; text-decoration: underline;">Volver a empezar</button>
            </div>
        </div>
    </div>

    <nav class="navbar">
        <a href="/" class="logo" id="reset-home">⚡ LaptopComparator</a>
        <div class="nav-links">
            <a href="/comparar" class="nav-vs-link">⚖️ Comparador VS</a>
            <span class="nav-btn" id="nav-top">Top Ventas</span>
            <span class="nav-btn" id="nav-gaming">Gaming</span>
            <span class="nav-btn" id="nav-student">Estudiantes</span>
            <a href="https://t.me/PreciosPortatilesES" target="_blank" style="background-color: #2AABEE; color: white; padding: 6px 14px; border-radius: 50px; text-decoration: none; font-weight: 700; font-size: 0.85rem; display: flex; align-items: center; gap: 5px; box-shadow: 0 4px 10px rgba(42, 171, 238, 0.3); transition: transform 0.2s; margin-left: 10px;" onmouseover="this.style.transform='scale(1.05)'" onmouseout="this.style.transform='scale(1)'">✈️ Ofertas</a>
        </div>
    </nav>

    <section class="hero">
        <div class="badge-pill">✨ Algoritmo v2.1 Actualizado</div>
        <h1>¿Miedo a tirar 500€ <br /> en un portátil lento?</h1>
        <p class="subtitle">
            Las tiendas intentan venderte stock viejo. Nosotros analizamos el hardware real.
            <br> <span style="color: #4ade80; font-weight: bold;">⚡ Encuentra el mejor calidad-precio en 3 clics.</span>
        </p>
        
        <div class="cta-container">
            <a href="https://t.me/PreciosPortatilesES" target="_blank" class="btn-telegram-hero">✈️ Únete al Canal de Ofertas</a>
            <a href="/comparar" class="btn-compare-hero">⚖️ Ir al Comparador VS</a>
        </div>
    </section>

    <div class="flash-deal-wrapper">
        <a href="https://amzn.to/49iQsJ7" target="_blank" class="flash-deal-card">
            <div class="flash-badge">🔥 TOP VENTAS</div>
            <div class="flash-content">
                <span class="flash-icon">🖱️</span>
                <div class="flash-text">
                    <h3>¡Ratón Logitech B100 por 4€!</h3>
                    <p>Precio ridículo. Ideal para salir del paso o tener de repuesto.</p>
                </div>
            </div>
            <div class="flash-price">
                <span class="old-price">12,00€</span>
                <span class="new-price">4,95€</span>
                <span class="btn-grab">VER OFERTA ➜</span>
            </div>
        </a>
    </div>

    <div class="main-container">
        <aside class="sidebar">
            <div class="filter-box">
                <span class="filter-title">Ordenar Por</span>
                <div class="filter-item" id="sort-score">⚡ Mejor Puntuación</div>
                <div class="filter-item" id="sort-price-asc">💰 Precio: Bajo a Alto</div>
                <div class="filter-item" id="sort-price-desc">💎 Precio: Alto a Bajo</div>
            </div>
            <div class="filter-box">
                <span class="filter-title">Rango de Precio</span>
                <div class="filter-item" id="filter-all">🔄 Ver Todos</div>
                <div class="filter-item" id="filter-under-500">📉 Menos de 500€</div>
                <div class="filter-item" id="filter-under-800">⚖️ Menos de 800€</div>
                <div class="filter-item" id="filter-over-1000">🚀 Gama Alta (+1000€)</div>
            </div>
        </aside>

        <main class="grid" id="laptops-grid">
            {laptops.map((laptop, index) => (
                <article 
                    class={`card ${index === 0 ? 'winner-card' : ''}`} 
                    data-price={laptop.price_eur} 
                    data-score={getScore(laptop.price_eur)} 
                    data-category={laptop.category}
                >
                    {/* SI ES EL PRIMERO, PONEMOS LA CORONA */}
                    {index === 0 && <div class="winner-badge-label">🏆 MEJOR OPCIÓN 2024</div>}

                    <div class="score-badge">
                        {getScore(laptop.price_eur)} <span class="score-label">PTS</span>
                    </div>
                    <img src={laptop.brand_img} alt={laptop.name} class="product-img" />
                    <h2>{laptop.name}</h2>
                    <div class="specs-grid">
                        <div class="spec-item">🧠 {laptop.specs.cpu}</div>
                        <div class="spec-item">💾 {laptop.specs.ram}</div>
                        <div class="spec-item">💿 {laptop.specs.storage}</div>
                        <div class="spec-item">⚖️ {laptop.specs.weight}</div>
                    </div>
                    <div class="price-row">
                        <div class="price-container">
                            <span class="old-price-anchor">{(laptop.price_eur * 1.2).toFixed(0)}€</span>
                            <span class="price-display">{laptop.price_eur} €</span>
                        </div>
                        <a href={`/laptops/${laptop.slug}`} class="btn-card">Ver Ficha</a>
                    </div>
                </article>
            ))}
        </main>
    </div>

    <footer class="site-footer">
        <div class="footer-content">
            <div class="footer-brand">
                <h3>LaptopComparator</h3>
                <p>&copy; {new Date().getFullYear()} - Calidad/Precio Garantizada.</p>
            </div>
            <div class="footer-links">
                <a href="#">Aviso Legal</a>
                <a href="#">Privacidad</a>
                <a href="#">Contacto</a>
            </div>
        </div>
        <div class="amazon-legal-box">
            <p class="legal-text">
                Esta web participa en el Programa de Afiliados de Amazon.es, un programa de publicidad para afiliados diseñado para ofrecer a sitios web un modo de obtener comisiones por publicidad, publicitando e incluyendo enlaces a Amazon.es.
                <br />
                En calidad de Afiliado de Amazon, obtengo ingresos por las compras adscritas que cumplen los requisitos aplicables.
                Amazon y el logotipo de Amazon son marcas comerciales de Amazon.com, Inc. o de sociedades de su grupo.
            </p>
        </div>
    </footer>

    <script define:vars={{laptops}}>
        // --- 1. LÓGICA DE FILTROS DE LA GRID ---
        const grid = document.getElementById('laptops-grid');
        // Importante: Actualizamos el selector para incluir 'winner-card' si es necesario, 
        // pero .card sigue siendo la clase base, así que funciona igual.
        const cards = Array.from(document.querySelectorAll('.card'));
        
        function filterByCategory(category) {
            cards.forEach(card => {
                const categories = card.dataset.category || "";
                if (categories.includes(category)) card.style.display = 'flex';
                else card.style.display = 'none';
            });
            grid.scrollIntoView({behavior: 'smooth'});
        }

        const navTop = document.getElementById('nav-top');
        const navGaming = document.getElementById('nav-gaming');
        const navStudent = document.getElementById('nav-student');
        if(navTop) navTop.addEventListener('click', () => filterByCategory('top'));
        if(navGaming) navGaming.addEventListener('click', () => filterByCategory('gaming'));
        if(navStudent) navStudent.addEventListener('click', () => filterByCategory('student'));
        
        document.getElementById('reset-home').addEventListener('click', () => {
            cards.forEach(card => card.style.display = 'flex');
            window.scrollTo({ top: 0, behavior: 'smooth' });
        });
        
        document.getElementById('sort-score').addEventListener('click', () => updateGrid(cards.sort((a, b) => b.dataset.score - a.dataset.score)));
        document.getElementById('sort-price-asc').addEventListener('click', () => updateGrid(cards.sort((a, b) => a.dataset.price - b.dataset.price)));
        document.getElementById('sort-price-desc').addEventListener('click', () => updateGrid(cards.sort((a, b) => b.dataset.price - a.dataset.price)));
        
        document.getElementById('filter-all').addEventListener('click', () => cards.forEach(c => c.style.display = 'flex'));
        document.getElementById('filter-under-500').addEventListener('click', () => filterByPrice(0, 500));
        document.getElementById('filter-under-800').addEventListener('click', () => filterByPrice(0, 800));
        document.getElementById('filter-over-1000').addEventListener('click', () => filterByPrice(1000, 99999));

        function filterByPrice(min, max) {
            cards.forEach(card => {
                const price = parseFloat(card.dataset.price);
                if (price >= min && price <= max) card.style.display = 'flex';
                else card.style.display = 'none';
            });
        }
        function updateGrid(sortedCards) {
            grid.innerHTML = '';
            sortedCards.forEach(card => { card.style.display = 'flex'; grid.appendChild(card); });
        }


        // --- 2. LÓGICA DEL WIZARD (RECOMENDADOR) ---
        let wizardData = { maxPrice: 9999, use: '' };
        
        // Abrir/Cerrar Modal
        const modal = document.getElementById('wizard-overlay');
        document.getElementById('wizard-trigger').addEventListener('click', () => modal.classList.add('active'));
        document.getElementById('close-wizard').addEventListener('click', () => modal.classList.remove('active'));

        // Hacer las funciones globales para que el HTML las vea
        window.nextStep = function(step, value) {
            if (step === 1) {
                wizardData.maxPrice = value;
                document.getElementById('step-1').classList.remove('active');
                document.getElementById('step-2').classList.add('active');
            } else if (step === 2) {
                wizardData.use = value;
                document.getElementById('step-2').classList.remove('active');
                document.getElementById('step-result').classList.add('active');
                calculateResult();
            }
        };

        window.resetWizard = function() {
            document.getElementById('step-result').classList.remove('active');
            document.getElementById('step-1').classList.add('active');
            wizardData = { maxPrice: 9999, use: '' };
        };

        function calculateResult() {
            document.getElementById('wizard-loading').style.display = 'block';
            document.getElementById('wizard-match').style.display = 'none';

            setTimeout(() => {
                // ALGORITMO DE FILTRADO
                let filtered = laptops.filter(l => l.price_eur <= wizardData.maxPrice);
                
                if (wizardData.use === 'gaming') {
                    let gaming = filtered.filter(l => l.category.includes('gaming') || l.name.toLowerCase().includes('rtx'));
                    if(gaming.length > 0) filtered = gaming;
                } else if (wizardData.use === 'student') {
                    filtered.sort((a,b) => a.price_eur - b.price_eur);
                } else if (wizardData.use === 'code') {
                    let pro = filtered.filter(l => l.specs.ram.includes('16GB'));
                    if(pro.length > 0) filtered = pro;
                }

                let winner = filtered.length > 0 ? filtered[0] : laptops[0];

                // --- GENERAR ENLACE AMAZON ---
                const amazonLink = `https://www.amazon.es/s?k=${winner.name.replace(/ /g, '+')}&tag=comparadorjai-21`;

                document.getElementById('wizard-loading').style.display = 'none';
                document.getElementById('wizard-match').style.display = 'block';
                
                document.getElementById('wiz-img').src = winner.brand_img;
                document.getElementById('wiz-name').innerText = winner.name;
                document.getElementById('wiz-price').innerText = winner.price_eur + ' €';
                
                const wizLinkBtn = document.getElementById('wiz-link');
                wizLinkBtn.href = amazonLink;
                wizLinkBtn.innerText = '🛒 Ver Oferta en Amazon';
                wizLinkBtn.target = '_blank'; 
                wizLinkBtn.rel = 'noopener noreferrer';

            }, 800);
        }
    </script>
</body>
</html>